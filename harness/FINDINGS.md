# FINDINGS — moved

The findings this harness produced live at the repository root, as the canonical
copy:

**[`../docs/FINDINGS_2026-08.md`](../docs/FINDINGS_2026-08.md)**

That document holds the verdicts per scenario, the field-by-field leak tables,
the PASS/FAIL results for cross-hop unlinkability, attenuation and cascading
revocation, the explicit limitations, and the questions for a cryptographer.

The raw run it is based on is committed here:
[`sample-output/run-output.txt`](sample-output/run-output.txt).

This file is a pointer only, kept so the two copies cannot drift apart.
